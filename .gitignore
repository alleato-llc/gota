# Build artifacts from runners (the harness builds these on demand)
runner
*.o
*.hi
*.class
__pycache__/

# Rust
target/

# Go
go.sum

# Zig
.zig-cache/
zig-out/

# Node
node_modules/
# The landing page's static output — generated by `npm run build`, published by
# the deploy (salpa builds it fresh); never committed.
dist/
# Copied from examples/report.html at build time by web/scripts/sync-report.mjs
# (npm prebuild), so the published page can't drift from the committed example.
web/public/report.html
