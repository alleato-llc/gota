{
  "name": "gota-web",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/sync-report.mjs",
    "dev": "astro dev",
    "prebuild": "node scripts/sync-report.mjs",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/preact": "^4.0.0",
    "astro": "^5.0.0",
    "preact": "^10.25.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
